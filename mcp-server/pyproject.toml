[project]
name = "resk-mcp-server"
version = "0.1.0"
description = "MCP server for RESK LLM Firewall — AI-configurable security platform"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27",
]

[project.scripts]
resk-mcp = "main:main"
